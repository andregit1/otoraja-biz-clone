<% content_for(:title, t('console.variant.add_variant')) %>
 
<div class="card">
  <%= form_with(model: @variant, url: console_variants_path, local: true) do |form| %>
    <%= render 'form', {variant: @variant, f: form} %>
  <% end %>
</div>
