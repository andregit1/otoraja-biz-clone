<% content_for(:title, t('console.maker.add_maker')) %>

<div class="card">
  <%= form_with(model: @maker, url: console_makers_path, local: true) do |form| %>
  <%= render 'form', {maker: @maker, f: form} %>
  <% end %>
</div>
