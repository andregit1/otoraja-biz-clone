<% content_for(:title, t('console.shop.new.title')) %>

<div class="card">
  <%= form_with(model: @shop, url:console_shops_path, local: true) do |form| %>
  <%= render 'form', {shop: @shop, f: form} %>
  <% end %>
</div>
