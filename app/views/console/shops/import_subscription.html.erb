<% content_for(:title, 'Shops Subscription Import') %>
<div id="alert"></div>
<div class="card">
  <%= form_with model: @shop_subscription_upload, url: console_shops_import_subscription_execution_path, multipart: true, remote: true do |form| %>
    <%= render 'form_upload', {shop_subscription_upload: @shop_subscription_upload, f: form} %>
  <% end %>
</div>

<div class="card" id="resultImport"></div>
