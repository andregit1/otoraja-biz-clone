<% content_for(:title, t('console.brand.edit_brand')) %>

<div class="card">
  <%= form_with(model: @brand, url: console_brand_path, local: true) do |form| %>
    <%= render 'form', {brand: @brand, f: form} %>
  <% end %>
</div>
