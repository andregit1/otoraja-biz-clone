<% content_for(:title, t('console.common.whatsapp')) %>

<div class="card">
  <%= form_with(model: @whats_app_template, url: console_whats_app_template_path, local: true) do |form| %>
  <%= render 'form', {template: @whats_app_template, f: form} %>
  <% end %>
</div>