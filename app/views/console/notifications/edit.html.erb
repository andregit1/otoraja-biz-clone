<% content_for(:title, t('console.notification.edit.title')) %>

<div class="card">
  <%= form_with(model: @notification, url: console_notification_path, local: true) do |form| %>
    <%= render 'form', {f: form} %>
  <% end %>
</div>
