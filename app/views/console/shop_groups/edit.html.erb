<% content_for(:title, t('console.shop_group.edit.title')) %>

<div class="card">
  <%= form_with(model: @shop_group, url:console_shop_group_path, local: true) do |form| %>
  <%= render 'form', {shop_group: @shop_group, f: form} %>
  <% end %>
</div>