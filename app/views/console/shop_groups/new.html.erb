<% content_for(:title, t('console.shop_group.new.title')) %>

<div class="card">
  <%= form_with(model: @shop_group, url:console_shop_groups_path, local: true) do |form| %>
  <%= render 'form', {shop_group: @shop_group, f: form} %>
  <% end %>
</div>