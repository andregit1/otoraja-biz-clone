<% content_for(:title, 'Edit User') %>
<div class="card">
  <%= form_with(model: @user, url:console_user_path, local: true) do |form| %>
  <%= render 'form', {user: @user, roles: @roles, f: form} %>
  <% end %>
</div>
