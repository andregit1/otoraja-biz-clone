<% content_for(:title, t('console.service.edit.title')) %>

<div class="card">
  <%= form_with(model: @service, url:console_service_path, local: true) do |form| %>
  <%= render 'form', {service: @service, f: form} %>
  <% end %>
</div>