<% content_for(:title, 'Edit User') %>
<div class="card">
  <%= form_with(model: @user, url: admin_user_path, local: true) do |form| %>
  <%= render 'form_edit', {user: @user, roles: @roles, f: form} %>
  <% end %>
</div>
