<% content_for(:title, t('admin.supplier.edit_supplier')) %>
<div class="card">
  <%= form_with(model: @supplier, url: admin_supplier_path, local: true) do |form| %>
  <%= render 'form', {supplier: @supplier, roles: @roles, f: form} %>
  <% end %>
</div>
