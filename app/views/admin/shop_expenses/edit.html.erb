<%= form_with(model: @shop_expense, url: admin_shop_expense_path, local: true) do |f| %>
    <%= render "form", {f: f} %>
<% end %>
<%= form_with(model: @supplier, url: admin_shop_expenses_create_supplier_path, :id => 'addModalSupplier' , remote: true) do |f| %>
    <%= f.hidden_field :shop_expense_id, :value => @shop_expense.id %>
    <%= render "modal_add_expense_report", {f: f}%>
<% end %>

